# x86-64
## RisckyMonke

This challenge is named `RisckyMonke`, and is located within module `x86-64`.

When users start this challenge, their environment will have:
- `/flag`
- `/challenge/RisckyMonke`

`/flag` is read-only by root, and `/challenge/RisckyMonke` is setuid root.
