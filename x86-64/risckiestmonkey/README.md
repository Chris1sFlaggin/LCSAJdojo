# x86-64
## risckiestmonkey

This challenge is named `risckiestmonkey`, and is located within module `x86-64`.

When users start this challenge, their environment will have:
- `/flag`
- `/challenge/risckiestmonkey`

`/flag` is read-only by root, and `/challenge/risckiestmonkey` is setuid root.
